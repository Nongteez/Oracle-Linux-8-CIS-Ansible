---
- name: CIS 4.4.3.2.1 - Ensure password number of changed characters is configured (difok)
  hosts: all
  become: true
  gather_facts: true

  vars:
    control_id: "4.4.3.2.1"
    control_name: "Ensure password number of changed characters is configured (difok)"
    difok_val: 4
    cis_enforce: true   # ค่าเริ่มต้น audit-only; จะให้แก้จริงใส่ -e "cis_enforce=true"

  tasks:
    - name: Detect current login user
      debug:
        msg: "Login as: {{ ansible_user_id }}, Become: {{ ansible_effective_user_id | default('root') }}"

    - name: Guard | Oracle Linux 8 family only
      ansible.builtin.assert:
        that:
          - ansible_facts['os_family'] == "RedHat"
          - ansible_facts['distribution'] in ["OracleLinux", "RedHat", "Rocky", "AlmaLinux"]
          - (ansible_facts['distribution_major_version'] | int) == 8
        fail_msg: "Unsupported OS. Expect OracleLinux 8.x family."

    - name: Audit | Read current difok (normalized)
      ansible.builtin.shell: |
        set -o pipefail
        if [ -f /etc/security/pwquality.conf ]; then
          awk -F= '/^[[:space:]]*difok[[:space:]]*=/ && $0 !~ /^[[:space:]]*#/ {
            gsub(/[[:space:]]/,"",$2); print $2
          }' /etc/security/pwquality.conf | head -n1
        fi
      args: { executable: /bin/bash }
      register: difok_current
      changed_when: false
      failed_when: false

    - name: Enforce | Set difok in /etc/security/pwquality.conf
      when: cis_enforce | bool
      ansible.builtin.lineinfile:
        path: /etc/security/pwquality.conf
        regexp: '^[[:space:]]*#?[[:space:]]*difok[[:space:]]*='
        line: "difok = {{ difok_val }}"
        create: true
        backup: true
        mode: "0644"
        insertafter: EOF
        state: present

    - name: Verify | difok equals expected
      ansible.builtin.shell: |
        set -o pipefail
        if [ -f /etc/security/pwquality.conf ]; then
          grep -E '^[[:space:]]*difok[[:space:]]*=[[:space:]]*{{ difok_val }}[[:space:]]*$' /etc/security/pwquality.conf || true
        fi
      args: { executable: /bin/bash }
      register: difok_verify
      changed_when: false
      failed_when: false

    - name: Build result for CSV export
      set_fact:
        new_result:
          control_id: "{{ control_id }}"
          name: "{{ control_name }}"
          command: "pwquality.conf -> difok={{ difok_val }}"
          expected: "difok = {{ difok_val }}"
          result: >-
            current={{ difok_current.stdout | default('') | trim }};
            verify_line={{ (difok_verify.stdout | default('Not Found')) | regex_replace('\n',' ') }};
          status: "{{ 'Pass' if (difok_verify.stdout | default('') | length > 0) else 'Fail' }}"
          host: "{{ inventory_hostname }}"
          ip: "{{ ansible_host | default(ansible_default_ipv4.address | default('N/A')) }}"

    - name: Append result to result_list
      set_fact:
        result_list: "{{ result_list | default([]) + [ new_result ] }}"

    - name: Add host result list to reporting group
      add_host:
        name: "{{ inventory_hostname }}"
        groups: cis_report
        result_list: "{{ result_list }}"

# ===== CSV Reporting (ตามเทมเพลตเดิม) =====
- name: Export CIS 4.4.3.2.1 Results to CSV
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Combine result_list from all hosts
      set_fact:
        combined_results: >-
          {{
            hostvars.values()
            | selectattr('result_list', 'defined')
            | map(attribute='result_list')
            | list
            | flatten
          }}

    - name: Export combined result list to CSV (Local)
      copy:
        content: |
          control_id,cis_name,command,expected_result,actual_result,host,ip,status
          {% for r in combined_results %}
          "{{ r.control_id }}","{{ r.name }}","{{ r.command }}","{{ r.expected }}","{{ r.result | replace(',', ';') }}","{{ r.host }}","{{ r.ip }}","{{ r.status }}"
          {% endfor %}
        dest: "./cis_4_4_3_2_1_report.csv"
